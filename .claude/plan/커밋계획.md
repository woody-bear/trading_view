# 커밋 계획

**작성일**: 2026-03-20 | **브랜치**: 001-ubb-pro-signal-system

## 커밋 전략

프로젝트 구조를 이해하기 쉽게 **계층별**로 나누어 커밋합니다.
나중에 특정 계층만 수정할 때 `git log` + `git blame`으로 관련 커밋을 빠르게 찾을 수 있도록 합니다.

---

## Commit 1: 프로젝트 설정 + 인프라

핵심: 이 커밋만으로 프로젝트를 clone하고 구조를 이해할 수 있어야 함.

```
포함 파일:
  .gitignore
  .env.example
  README.md
  CLAUDE.md
  scripts/
  .specify/
  specs/
```

```
커밋 메시지:
chore: project setup — gitignore, env config, README, CLAUDE.md
```

---

## Commit 2: 백엔드 코어 — DB 모델 + 설정 + 마이그레이션

핵심: 데이터 계층. 이 커밋으로 DB 스키마와 설정 구조를 이해할 수 있어야 함.

```
포함 파일:
  backend/config.py
  backend/database.py
  backend/models.py
  backend/requirements.txt
  backend/alembic.ini
  backend/alembic/
```

```
커밋 메시지:
feat(backend): database models, config, and migrations — 9 tables
```

---

## Commit 3: 백엔드 지표 엔진 + 데이터 수집

핵심: 비즈니스 로직 계층. 신호 판정 + 데이터 fetcher.

```
포함 파일:
  backend/indicators/          # signal_engine, bollinger, rsi, macd, ema, volume
  backend/fetchers/            # base, kr_fetcher, us_fetcher, crypto_fetcher
```

```
커밋 메시지:
feat(backend): indicator engine (BB, RSI, MACD, EMA, squeeze) + data fetchers
```

---

## Commit 4: 백엔드 서비스 계층

핵심: 스캐너, 실시간 가격, 텔레그램, 한투 API, 차트 캐시 등 서비스 모듈.

```
포함 파일:
  backend/services/            # scanner, unified_scanner, chart_cache, stock_master,
                               # price_feed, kis_client, kis_websocket, telegram_bot,
                               # forex_analyzer, chart_scanner, market_scanner,
                               # symbol_validator
  backend/scheduler.py
```

```
커밋 메시지:
feat(backend): services — scanner, KIS realtime, telegram, chart cache, stock master
```

---

## Commit 5: 백엔드 API 라우트 + 앱 엔트리포인트

핵심: HTTP 엔드포인트 계층. 프론트엔드와의 인터페이스.

```
포함 파일:
  backend/app.py
  backend/routes/              # signals, watchlist, charts, quick_chart, prices,
                               # financials, search, market_scan, settings, forex,
                               # websocket, webhook, system
```

```
커밋 메시지:
feat(backend): API routes — signals, charts, prices, search, forex, settings, websocket
```

---

## Commit 6: 프론트엔드 코어 — 설정 + 타입 + 상태관리

핵심: 프론트엔드 기반 구조.

```
포함 파일:
  frontend/package.json
  frontend/pnpm-lock.yaml
  frontend/tsconfig*.json
  frontend/vite.config.ts
  frontend/tailwind.config.*
  frontend/index.html
  frontend/src/main.tsx
  frontend/src/App.tsx
  frontend/src/index.css
  frontend/src/types/index.ts
  frontend/src/api/client.ts
  frontend/src/stores/signalStore.ts
  frontend/src/utils/format.ts
  frontend/src/hooks/
```

```
커밋 메시지:
feat(frontend): core setup — routing, API client, hooks, stores, types
```

---

## Commit 7: 프론트엔드 컴포넌트 + 페이지

핵심: UI 계층. 모든 페이지와 컴포넌트.

```
포함 파일:
  frontend/src/components/     # SignalCard, BottomNav, charts/*
  frontend/src/pages/          # Dashboard, Scan, SignalDetail, TopPicks, Forex, Settings
  frontend/src/assets/
```

```
커밋 메시지:
feat(frontend): pages + components — dashboard, detail, scan, forex, settings, mobile nav
```

---

## Commit 8: 프론트엔드 빌드 결과물

핵심: 운영 배포용 정적 파일.

```
포함 파일:
  frontend/dist/               # 빌드 결과물 (선택적 — .gitignore에 추가 가능)
```

```
커밋 메시지:
build: frontend production build
```

> **참고**: `frontend/dist/`를 .gitignore에 추가하고 빌드는 배포 시점에 하는 것이 일반적.
> 하지만 Mac Mini 로컬 서버에서 `git pull` 후 바로 서빙하려면 포함하는 것도 방법.

---

## Commit 9: 문서 + 계획

핵심: 프로젝트 이해를 돕는 문서.

```
포함 파일:
  .claude/docs/                # ERD, 기능명세서 4개
  .claude/plan/                # 작업 계획 문서들
```

```
커밋 메시지:
docs: ERD, feature specs, and implementation plans
```

---

## 커밋 순서 요약

| # | 범위 | 메시지 |
|---|------|--------|
| 1 | 인프라 | `chore: project setup` |
| 2 | DB/설정 | `feat(backend): database models, config, migrations` |
| 3 | 지표/수집 | `feat(backend): indicator engine + data fetchers` |
| 4 | 서비스 | `feat(backend): services — scanner, KIS, telegram, cache` |
| 5 | API | `feat(backend): API routes` |
| 6 | FE 코어 | `feat(frontend): core setup` |
| 7 | FE UI | `feat(frontend): pages + components` |
| 8 | 빌드 | `build: frontend production build` (선택) |
| 9 | 문서 | `docs: ERD, feature specs, plans` |

## 주의사항

- `.env` 파일은 `.gitignore`에 포함 — 커밋하지 않음
- `backend/data/*.db` — 커밋하지 않음
- `frontend/node_modules/` — 커밋하지 않음
- `backend/.venv/` — 커밋하지 않음
- `frontend/dist/`는 `.gitignore`에 추가 권장
