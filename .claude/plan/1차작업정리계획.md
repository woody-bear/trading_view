# 1차 작업 정리 계획

**작성일**: 2026-03-20 | **상태**: 계획

## 분석 결과 요약

전체 소스코드를 분석한 결과, 코드베이스가 비교적 깨끗한 상태입니다.

| 항목 | 결과 | 조치 |
|------|------|------|
| 사용 안 하는 파일 | `QuickChart.tsx` 1개 | 삭제 |
| 사용 안 하는 폴더 | 없음 | - |
| 사용 안 하는 함수 | 없음 | - |
| 사용 안 하는 CSS | 없음 | - |
| 중복 함수 | `fmtPrice` 4곳 중복 | 유틸리티로 통합 |
| 보안 노출 | 없음 (`.env`로 관리 중) | - |

---

## Task 1: 사용 안 하는 파일 삭제

### `frontend/src/pages/QuickChart.tsx`
- **상태**: App.tsx에서 import/라우트 제거됨 (이전 작업에서)
- **참조**: 어디서도 import 안 됨
- **조치**: 파일 삭제

### `frontend/src/pages/AddSymbol.tsx`
- **확인 필요**: App.tsx에 라우트 없음. 다른 곳에서 import되는지 확인 후 삭제

---

## Task 2: 중복 함수 통합 — `fmtPrice`

동일한 가격 포맷팅 함수가 4곳에 중복 정의:

| 파일 | 시그니처 | 차이 |
|------|---------|------|
| `SignalCard.tsx:8` | `fmtPrice(price, market)` | KR만 체크 |
| `Dashboard.tsx:18` | `fmtPrice(price, market?)` | KR/KOSPI/KOSDAQ 체크 |
| `TopPicks.tsx:9` | `fmtPrice(price, market?)` | KR/KOSPI/KOSDAQ 체크 |
| `SignalDetail.tsx:~178` | `fmtPrice(price)` | 컴포넌트 내부 변수 참조 |

### 조치
1. `frontend/src/utils/format.ts` 신규 생성
2. 통합 `fmtPrice(price, market?)` 함수 export
3. 4개 파일에서 로컬 정의 제거 → import로 교체

```typescript
// frontend/src/utils/format.ts
export const fmtPrice = (price: number, market?: string) => {
  if (market === 'KR' || market === 'KOSPI' || market === 'KOSDAQ')
    return price?.toLocaleString(undefined, { maximumFractionDigits: 0 })
  return price?.toLocaleString(undefined, { maximumFractionDigits: 2 })
}
```

---

## Task 3: 보안 점검 결과

### 확인된 보안 사항 (모두 정상)

| 항목 | 관리 방식 | 상태 |
|------|----------|------|
| `TELEGRAM_BOT_TOKEN` | `.env` 파일 | OK |
| `TELEGRAM_CHAT_ID` | `.env` 파일 | OK |
| `KIS_APP_KEY` | `.env` 파일 | OK |
| `KIS_APP_SECRET` | `.env` 파일 | OK |
| `KIS_ACCOUNT_NO` | `.env` 파일 | OK |
| `TV_WEBHOOK_SECRET` | `.env` 파일 | OK |
| `BINANCE_API_KEY/SECRET` | `.env` 파일 | OK |
| `DATABASE_URL` | `.env` 파일 | OK |
| `.env` | `.gitignore`에 포함 | OK |
| Settings UI | 키 값 마스킹(`***`) 처리 | OK |

### `.gitignore` 확인

```
.env
.env.local
.env.*.local
backend/data/*.db
```

→ 민감 파일이 Git에 포함되지 않도록 설정됨.

---

## Task 4: 커밋 전 빌드/테스트 확인

1. `cd frontend && pnpm build` — 빌드 에러 없음 확인
2. `cd backend && python -c "from app import app"` — 모듈 임포트 확인
3. `alembic upgrade head` — 마이그레이션 최신 확인
4. 서버 시작 → 주요 API 동작 확인

---

## 작업 순서

1. QuickChart.tsx 삭제
2. fmtPrice 유틸리티 통합
3. 빌드 확인
4. 커밋

### 예상 소요: 약 30분
